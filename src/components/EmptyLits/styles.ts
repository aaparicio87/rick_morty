import { StyleSheet } from "react-native";

export default StyleSheet.create({
    emptyContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 50,
      },
      emptyText: {
        marginTop: 20,
        fontSize: 18,
        color: 'gray',
      },
})